const loginView = defineComponent({
  name: 'LoginView',
  setup() {
    return () => (
      <div>
        <h1>Login</h1>
        <div>
          <button>Login</button>
        </div>
      </div>
    )
  }
})

export default loginView
