export interface SettingConfig {
  isShowDraw: boolean
  isShowLogo: boolean
  isFixedHeader: boolean
}

export default {
  isShowDraw: false,
  isShowLogo: true,
  isFixedHeader: true
} as SettingConfig
