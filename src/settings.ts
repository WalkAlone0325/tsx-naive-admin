export interface SettingConfig {
  isShowDraw: boolean
  isShowLogo: boolean
  isFixedHeader: boolean
}

export default {
  isShowDraw: false,
  isShowLogo: false,
  isFixedHeader: true
} as SettingConfig
